# Starknet JSON-RPC endpoint used for reads (public balances, receipts, events).
# Cartridge serves spec 0.10.2 consistently. Avoid rpc.starknet.lava.build: it is a
# load-balanced mixed pool that intermittently answers with spec 0.8.1 and breaks
# 0.10-style calls.
NEXT_PUBLIC_STARKNET_RPC_URL=https://api.cartridge.gg/x/starknet/mainnet

# STRK20 privacy pool (mainnet).
NEXT_PUBLIC_STRK20_POOL_ADDRESS=0x040337b1af3c663e86e333bab5a4b28da8d4652a15a69beee2b677776ffe812a

# Cordon PolicyGate anonymizer, deployed on Starknet mainnet. The gate reports the
# registries it was constructed with, so they are read from chain rather than configured.
NEXT_PUBLIC_CORDON_GATE_ADDRESS=0x061c734fe518f4c1a0e46d3d2a35b4ff1ab0df17dec510cff401d25e67dfc6b2

# The published policy the Pay screen settles under in live mode. Unset means the
# screen reports the missing precondition rather than settling under a guess.
# PAY_ACCREDITED_V1 caps a single settlement at 3 STRK and a subject at 9 STRK per hour.
NEXT_PUBLIC_CORDON_POLICY_ID=PAY_ACCREDITED_V1

# The pool user a live demo payment credits.
NEXT_PUBLIC_CORDON_PAYEE=

# The policy a two-step settlement is funded under, and the one the payee is judged against when
# they claim it. Unset means the Pay screen offers only the direct leg.
NEXT_PUBLIC_CORDON_SETTLE_POLICY_ID=SETTLE_ACCREDITED_V1
NEXT_PUBLIC_CORDON_CLAIM_POLICY_ID=RECV_KYC_L2_V1

# Where the issuer service answers, e.g. http://localhost:8787 when running it locally.
# Unset in the published build: the issuer holds a signing key and belongs on a host its operator
# controls, so the console says issuance is unavailable rather than pretending to reach one.
NEXT_PUBLIC_CORDON_ISSUER_URL=
